spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/keycloak_db
    username: admin
    password: @9018*
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081
